# BE-01: Nginx на хост-машине — единственная публичная точка входа (SR-BE01-006).
# Разместить в /etc/nginx/sites-available/ (или аналог) и включить сайт.
# Контейнеры слушают только 127.0.0.1 (SR-BE01-007), доступны только этому Nginx.
#
# Важно: X-Real-IP и X-Forwarded-For обязательны для корректного IP в audit_log и rate-limit.
# Без них бэкенд видит только IP прокси (172.19.0.1 в Docker), а не реальный IP клиента.

server {
    listen 80;
    server_name _;   # заменить на домен в production

    # Статика (React) — контейнер frontend
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API — контейнер backend (без слэша в proxy_pass, чтобы URI /api/... передавался как есть)
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Для HTTPS: раскомментировать и настроить сертификаты (например, Let's Encrypt).
# server {
#     listen 443 ssl;
#     server_name your-domain.example;
#     ssl_certificate     /etc/ssl/certs/...;
#     ssl_certificate_key /etc/ssl/private/...;
#     location / { proxy_pass http://127.0.0.1:8080; ... }
#     location /api/ { proxy_pass http://127.0.0.1:8000/; ... }
# }
