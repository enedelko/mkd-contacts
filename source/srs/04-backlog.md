# Остальные фичи SRS (порядок как в монолите)

Фичи: FE-04, FE-03, ADM-01, VAL-01, BE-02, BE-03, BE-04, CORE-02, CORE-03, CORE-04, FE-01, FE-02, FE-05, BOT-01…04, ADM-02…05, OPS-01, OPS-02, CORE-06, OPS-03.

---

## FE-04 — Форма сбора данных (UI)

### 1. Архитектурный анализ (CoT)
Форма реализуется на Client (React SPA): поля ввода, отправка на Backend. Backend (Logic Module) принимает данные, проверяет лимиты и капчу, сохраняет в БД. Требуется интеграция с капчей (Turnstile) и блоком согласий по 152-ФЗ.

### 2. Описание и цель
UI-форма для сбора контактов и позиции по ОСС: чекбокс «Я собственник», контакт (телефон, Telegram, email), позиция (ЗА да/нет), формат (бумага/электронно), чекбокс «Зарегистрирован в Электронном Доме», обязательные согласия на ОПД и ссылка на Политику. Цель — сбор данных в соответствии с БТ и минимизацией ПДн.

### 3. Функциональные требования
* **SR-FE04-001:** Форма должна содержать поле выбора помещения (читаемое отображение выбранного помещения после выбора по карте/фильтрам).
* **SR-FE04-002:** Форма должна содержать чекбокс «Я собственник» (обязательный для отправки).
* **SR-FE04-003:** Форма должна содержать поля контакта: хотя бы одно из полей «Телефон», «Telegram», «Email» обязательно для заполнения.
* **SR-FE04-004:** Форма должна содержать выбор позиции по ОСС: «ЗА» (Да/Нет).
* **SR-FE04-005:** Форма должна содержать выбор формата голосования: Бумага или Электронно.
* **SR-FE04-006:** Форма должна содержать чекбокс «Зарегистрирован в Электронном Доме».
* **SR-FE04-007:** Форма должна содержать обязательные чекбоксы согласия на обработку ПДн и ссылку на Политику конфиденциальности.
* **SR-FE04-008:** Форма должна инициировать отображение виджета капчи (Turnstile) перед отправкой.
* **SR-FE04-009:** Клиент должен отправлять данные на сервер только после успешного прохождения капчи и валидации полей на стороне клиента.
* **SR-FE04-010:** При успешной отправке форма должна показывать сообщение об успехе; при ошибке — сообщение об ошибке без раскрытия лишних деталей.

### 4. Сценарий использования
**Триггер:** Пользователь выбрал помещение и открыл форму.
**Main Success Scenario:**
1. Пользователь заполняет поля, отмечает согласия, проходит капчу и нажимает «Отправить».
2. Клиент валидирует данные и отправляет запрос на API.
3. Сервер проверяет лимиты и капчу, сохраняет анкету и возвращает успех.
4. Пользователь видит подтверждение.
**Alternative Flows:**
* **AF-1 (Ошибка валидации):** При невалидных полях или непройденной капче клиент не отправляет запрос и отображает ошибки у полей/капчи.
* **AF-2 (Лимит 10 записей/час):** Сервер возвращает 429; клиент отображает сообщение о превышении лимита и возможности повторить позже.

### 5. Модель данных
**Update/новые сущности:** Анкета/контакт (contact_submission): связь с помещением (id помещения), флаг собственника, телефон (шифр), email (шифр), telegram_id (шифр), позиция_за (bool), формат_голоса, в_электронном_доме (bool), согласия (метаданные/версия политики), дата_создания, ip, статус (например: невалидирован/валидирован/неактуальен).

### 6. Интеграции и API
* **Method:** `POST /api/submit`
* **Request:**
```json
{
  "premise_id": "uuid_or_cadastral",
  "is_owner": true,
  "phone": "+7...",
  "email": "user@example.com",
  "telegram_id": null,
  "vote_for": true,
  "vote_format": "electronic",
  "registered_ed": false,
  "consent_version": "1.0",
  "captcha_token": "turnstile_response_token"
}
```
* **Response (успех):**
```json
{
  "success": true,
  "message": "Данные приняты"
}
```
* **Response (ошибка лимита):**
```json
{
  "detail": "Rate limit exceeded",
  "retry_after": 3600
}
```

### 7. Нефункциональные требования
* **Security:** Передача только по HTTPS; контакты на сервере сохраняются в зашифрованном виде (см. BE-02). Капча обязательна.
* **Logging:** Не логировать значения полей с ПДн в открытом виде; логировать только факт отправки и premise_id.

---

## FE-03 — Базовый поиск: каскадные фильтры

### 1. Архитектурный анализ (CoT)
Реализуется на Frontend (React): последовательный выбор Подъезд → Этаж → Тип помещения → Номер. Данные для вариантов фильтров запрашиваются с Backend из PostgreSQL (на основе реестра CORE-01). Служит текстовой альтернативой SVG-карте.

### 2. Описание и цель
Каскадный поиск помещения: выбор из списков «Подъезд» → «Этаж» → «Тип помещения» → «Номер». Цель — дать пользователю возможность выбрать помещение без карты (слабые устройства, доступность).

### 3. Функциональные требования
* **SR-FE03-001:** Система должна предоставлять API для получения списка уникальных подъездов по объекту (МКД/кадастр дома).
* **SR-FE03-002:** Система должна предоставлять API для получения списка этажей по выбранному подъезду.
* **SR-FE03-003:** Система должна предоставлять API для получения списка типов помещений по подъезду и этажу.
* **SR-FE03-004:** Система должна предоставлять API для получения списка номеров помещений по подъезду, этажу и типу.
* **SR-FE03-005:** После выбора номера система должна однозначно определять помещение (идентификатор для последующей формы FE-04).
* **SR-FE03-006:** Клиент должен отображать каскадные выпадающие списки (или эквивалент) и обновлять следующие уровни при смене предыдущего.

### 4. Сценарий использования
**Триггер:** Пользователь открывает страницу поиска помещения (без карты).
**Main Success Scenario:**
1. Пользователь выбирает подъезд → этаж → тип → номер.
2. Клиент запрашивает с сервера варианты для каждого уровня.
3. После выбора номера отображается выбранное помещение и возможность перейти к форме (FE-04).
**Alternative Flows:**
* **AF-1 (Нет данных):** Если реестр не загружен, API возвращает пустые списки; клиент отображает сообщение «Данные по дому пока не загружены».
* **AF-2 (Таймаут):** При таймауте запроса клиент отображает сообщение об ошибке и предлагает повторить.

### 5. Модель данных
Используется модель помещений из CORE-01. Достаточно атрибутов: подъезд, этаж, тип_помещения, номер, id/кадастровый_номер.

### 6. Интеграции и API
* **Method:** `GET /api/premises/filters?building_id={id}&entrance={}&floor={}&type={}`
* **Response (список подъездов):**
```json
{
  "entrances": ["1", "2", "3"]
}
```
* **Response (этажи):**
```json
{
  "floors": [1, 2, 3, 4, 5]
}
```
* **Response (типы):**
```json
{
  "types": ["квартира", "машиноместо", "кладовка"]
}
```
* **Response (номера и итоговый premise_id):**
```json
{
  "premises": [
    { "number": "45", "premise_id": "uuid_or_cadastral" }
  ]
}
```

### 7. Нефункциональные требования
* **Performance:** Ответы списков — кешируемые на уровне HTTP (короткий Cache-Control) или быстрые запросы к БД с индексами по подъезд/этаж/тип.

---

## ADM-01 — Auth: Telegram OAuth (white-list)

### 1. Архитектурный анализ (CoT)
Реализуется в Auth Module на Backend (FastAPI): OAuth2 flow с Telegram как провайдером, выдача JWT после успешной авторизации. Сверка с белым списком (БД или конфиг) определяет, выдавать ли токен администратора. Архитектура предполагает хранение JWT на клиенте и передачу в заголовке Authorization.

### 2. Описание и цель
Авторизация администраторов и суперадминов через Telegram OAuth с проверкой по белому списку. Цель — ограничить доступ к админ-функциям только доверенными лицами.

### 3. Функциональные требования
* **SR-ADM01-001:** Система должна инициировать OAuth-авторизацию через Telegram (редирект на Telegram OAuth или использование Telegram Login Widget).
* **SR-ADM01-002:** Система должна получать от Telegram идентификатор пользователя (telegram_id) и при необходимости имя/username после успешной авторизации.
* **SR-ADM01-003:** Система должна проверять telegram_id по белому списку перед выдачей JWT.
* **SR-ADM01-004:** Если telegram_id отсутствует в белом списке, система должна возвращать отказ в доступе и не выдавать JWT.
* **SR-ADM01-005:** Система должна выдавать JWT с указанием роли (administrator / super_administrator) в соответствии с белым списком.
* **SR-ADM01-006:** Система должна защищать эндпоинты админки проверкой JWT и роли.

### 4. Сценарий использования
**Триггер:** Пользователь нажимает «Войти через Telegram» в админ-интерфейсе.
**Main Success Scenario:**
1. Пользователь переходит в Telegram OAuth и подтверждает доступ.
2. Сервер получает callback с данными, проверяет white-list, выдаёт JWT.
3. Клиент сохраняет JWT и перенаправляет в админ-панель.
**Alternative Flows:**
* **AF-1 (Не в white-list):** Сервер возвращает 403 с сообщением «Доступ запрещён».
* **AF-2 (Ошибка Telegram/сеть):** Сервер возвращает 502/503; клиент отображает сообщение «Повторите попытку позже».

### 5. Модель данных
**Новые сущности:** Белый список админов: telegram_id, роль (admin/super_admin). Хранение в БД или конфиге (для суперадминов — см. ADM-04).

### 6. Интеграции и API
* **Method:** `GET /api/auth/telegram/callback` (или POST с телом после redirect)
* **Query/body:** Параметры, возвращаемые Telegram OAuth (hash, id, first_name, username и т.д.).
* **Response (успех):**
```json
{
  "access_token": "eyJ...",
  "token_type": "bearer",
  "role": "administrator"
}
```
* **Response (отказ):**
```json
{
  "detail": "Access denied: not in white-list"
}
```

### 7. Нефункциональные требования
* **Security:** JWT с ограниченным временем жизни; белый список хранится под контролем (БД с доступом только для суперадмина/конфиг).
* **Logging:** Логировать факт входа (telegram_id, роль, время) без хранения токена.

---

## VAL-01 — Валидация: смена статуса контакта на помещении

### 1. Архитектурный анализ (CoT)
Реализуется в Logic Module: изменение статуса записи контакта (например, на «валидирован» или «неактуальный»). Доступ только для авторизованного администратора. Влияет на отображение легенды на карте (FE-02) и расчёт кворума (CORE-04, CORE-06).

### 2. Описание и цель
Функционал смены статуса контакта по помещению (валидирован / неактуальный и т.д.) для модерации данных и отражения на карте и отчётах.

### 3. Функциональные требования
* **SR-VAL01-001:** Система должна предоставлять API для установки статуса контакта на «валидирован» (доступ только для администратора/суперадмина).
* **SR-VAL01-002:** Система должна предоставлять API для установки статуса контакта на «неактуальный» (доступ только для администратора/суперадмина).
* **SR-VAL01-003:** Система должна однозначно идентифицировать запись контакта по её идентификатору (и при необходимости по помещению).
* **SR-VAL01-004:** После смены статуса система должна сохранять изменение в БД в рамках транзакции.
* **SR-VAL01-005:** Система должна записывать в аудит-лог факт смены статуса, идентификатор записи, старый и новый статус, идентификатор администратора и время (см. BE-03).

### 4. Сценарий использования
**Триггер:** Администратор подтверждает контакт или помечает запись как неактуальную.
**Main Success Scenario:**
1. Администратор выбирает запись и действие (валидировать / неактуально).
2. Клиент отправляет запрос с JWT.
3. Сервер проверяет права, обновляет статус, пишет аудит, возвращает успех.
**Alternative Flows:**
* **AF-1 (Запись не найдена):** Сервер возвращает 404.
* **AF-2 (Нет прав):** Сервер возвращает 403.

### 5. Модель данных
**Update:** Контакт/анкета — атрибут статус (например: pending / validated / inactive). Аудит-таблица: entity_type, entity_id, action, old_value, new_value, user_id, ip, timestamp.

### 6. Интеграции и API
* **Method:** `PATCH /api/admin/contacts/{contact_id}/status`
* **Request:**
```json
{
  "status": "validated"
}
```
* **Response (успех):**
```json
{
  "contact_id": "uuid",
  "status": "validated"
}
```

### 7. Нефункциональные требования
* **Security:** Только роль administrator или super_administrator. Аудит обязателен (BE-03).

---

## BE-03 — Аудит-лог: INSERT, UPDATE, DELETE, SELECT

### 1. Архитектурный анализ (CoT)
Реализуется на Backend: перехват изменений (INSERT/UPDATE/DELETE) и при необходимости избранных SELECT (чтение контактов админом). Запись в отдельную таблицу или сервис логов в PostgreSQL. БТ требует логирование действий и чтения, IP-адреса.

### 2. Описание и цель
Централизованное логирование изменений данных и фактов чтения чувствительных данных для аудита и соответствия требованиям БТ.

### 3. Функциональные требования
* **SR-BE03-001:** Система должна логировать каждую операцию INSERT в таблицы, содержащие ПДн или критичные для кворума данные (контакты, помещения).
* **SR-BE03-002:** Система должна логировать каждую операцию UPDATE по таким таблицам с фиксацией старых и новых значений (или хешей/меток для ПДн).
* **SR-BE03-003:** Система должна логировать каждую операцию DELETE с идентификатором записи и контекстом.
* **SR-BE03-004:** Система должна логировать факт SELECT (чтения) записей с контактами при доступе администратора (какие записи, кто, когда).
* **SR-BE03-005:** В каждую запись лога система должна записывать: тип операции, сущность, идентификатор записи, идентификатор пользователя (если есть), IP-адрес, метка времени.
* **SR-BE03-006:** Система не должна записывать в лог сами значения ПДн (телефон/email в открытом виде); допускается идентификатор записи и тип поля.

### 4. Сценарий использования
**Триггер:** Любое изменение или авторизованное чтение контактов.
**Main Success Scenario:**
1. Происходит INSERT/UPDATE/DELETE или чтение контакта админом.
2. Модуль аудита получает событие и записывает запись в таблицу логов.
**Alternative Flows:**
* **AF-1 (Переполнение лога):** Политика хранения (срок/размер) должна быть определена; при недоступности таблицы логов — логировать в fallback (файл) и не блокировать основную операцию.
* **AF-2 (Высокая нагрузка):** Запись в лог не должна существенно увеличивать время ответа API (асинхронная запись или быстрая вставка).

### 5. Модель данных
**Новые сущности:** audit_log: id, operation (INSERT/UPDATE/DELETE/SELECT), entity_type, entity_id, user_id, ip, timestamp, old_value_meta (без ПДн), new_value_meta (без ПДн).

### 6. Интеграции и API
Внутренняя реализация. API для просмотра логов — отдельный эндпоинт для админа (например, GET /api/admin/audit с фильтрами по времени, типу, пользователю).

### 7. Нефункциональные требования
* **Performance:** Асинхронная или быстрая запись в лог; индексы по timestamp, user_id, entity_type.
* **Security:** Доступ к просмотру логов только для администраторов; не хранить ПДн в логе.

---

## BE-04 — Защита: Rate Limiting (10 зап/час) и капча (Turnstile)

### 1. Архитектурный анализ (CoT)
Rate limiting реализуется на Backend (middleware или зависимость FastAPI) по IP. Капча — на Frontend виджет Turnstile; токен передаётся в теле запроса отправки анкеты и проверяется на Backend через API Turnstile. Оба механизма защищают от спама и ботов.

### 2. Описание и цель
Ограничение количества отправок анкет с одного IP (10 в час) и обязательная проверка капчи (Cloudflare Turnstile) при отправке формы.

### 3. Функциональные требования
* **SR-BE04-001:** Система должна ограничивать количество успешных запросов на создание анкеты (POST /api/submit) с одного IP-адреса до 10 в час.
* **SR-BE04-002:** При превышении лимита система должна возвращать HTTP 429 с заголовком Retry-After (или аналог) и не сохранять анкету.
* **SR-BE04-003:** Система должна принимать в запросе отправки анкеты токен капчи (Turnstile response token).
* **SR-BE04-004:** Система должна проверять токен капчи через API Turnstile перед сохранением анкеты.
* **SR-BE04-005:** Если проверка капчи не пройдена, система должна возвращать ошибку (например, 400) и не сохранять анкету.
* **SR-BE04-006:** Лимит 10 записей в час должен учитывать только успешно созданные анкеты (или 🤔 все попытки — уточнить; по БТ «лимит до 10 невалидированных записей» — допущение: 10 попыток/час с IP).

### 4. Сценарий использования
**Триггер:** Пользователь отправляет форму или злоумышленник выполняет массовые запросы.
**Main Success Scenario:**
1. Пользователь проходит капчу и отправляет форму.
2. Сервер проверяет капчу, проверяет счётчик по IP, создаёт запись и возвращает успех.
**Alternative Flows:**
* **AF-1 (Капча не пройдена):** Сервер возвращает 400, анкета не сохраняется.
* **AF-2 (Превышен лимит):** Сервер возвращает 429, клиент показывает сообщение о лимите.

### 5. Модель данных
Для rate limit: счётчик по IP (in-memory или Redis; в arch Redis не указан — 🤔 допущение: in-memory или таблица в PostgreSQL с TTL/очисткой по времени).

### 6. Интеграции и API
* **Внешняя интеграция:** Backend вызывает API Turnstile для проверки токена (siteverify).
* **Поведение API:** POST /api/submit без валидного captcha_token или при 429 — см. разделы выше.

### 7. Нефункциональные требования
* **Security:** Rate limit по IP; при использовании прокси/общего IP возможны ложные срабатывания — документировать.
* ⚠️ **Риск:** При нескольких серверах in-memory rate limit не общий — нужен общий хранилищ (Redis) или единая точка входа.

---

## CORE-02 — Валидация и лимиты: типы, лимиты (10/10), дедупликация

### 1. Архитектурный анализ (CoT)
Реализуется в Logic Module при приёме анкеты (POST /api/submit): проверка типов полей (телефон, email, telegram), проверка лимита невалидированных записей на помещение (10) и общего лимита с IP (уже BE-04), дедупликация (один контакт на помещение или политика «последний перезаписывает» — по БТ уточнить). Связан с потоком загрузки реестров (LOST-02, CORE-01): валидация форматов и правил применяется и к данным импорта; приоритет фичи повышен в связи с критичностью контура импорта для навигации и аналитики.

### 2. Описание и цель
Валидация форматов контактов, контроль лимита невалидированных записей на помещение (до 10) и дедупликация для предотвращения спама и дублей.

### 3. Функциональные требования
* **SR-CORE02-001:** Система должна проверять формат поля «телефон» (допустимые символы, длина, маска) перед сохранением.
* **SR-CORE02-002:** Система должна проверять формат поля «email» (синтаксис email) перед сохранением.
* **SR-CORE02-003:** Система должна проверять формат поля «telegram_id» (если передаётся) перед сохранением.
* **SR-CORE02-004:** Система должна отклонять анкету, если для выбранного помещения уже существует 10 невалидированных записей (лимит «10 невалидированных» на помещение).
* **SR-CORE02-005:** Система должна применять правило дедупликации: при наличии существующей записи по тому же помещению и тому же контакту (или ключу) — обновлять существующую запись или отклонять с сообщением (политика по БТ: обновление или отказ — 🤔 допущение: обновление с сохранением истории через аудит).
* **SR-CORE02-006:** При нарушении лимита или валидации система должна возвращать понятный код ошибки и сообщение без раскрытия внутренней структуры.

### 4. Сценарий использования
**Триггер:** Отправка анкеты (POST /api/submit).
**Main Success Scenario:**
1. Сервер проверяет форматы полей, лимит невалидированных по помещению, дедупликацию.
2. При успехе сохраняет/обновляет запись.
**Alternative Flows:**
* **AF-1 (Ошибка валидации формата):** Возврат 400 с указанием поля и типа ошибки.
* **AF-2 (Лимит 10 невалидированных на помещение):** Возврат 400/409 с сообщением «Достигнут лимит невалидированных записей по этому помещению».

### 5. Модель данных
Используются существующие сущности помещений и контактов. Для дедупликации нужен критерий уникальности (помещение + хеш контакта или помещение + тип контакта).

### 6. Интеграции и API
Ответы ошибок в рамках POST /api/submit:
```json
{
  "detail": "Validation failed",
  "errors": [
    { "field": "phone", "message": "Invalid format" }
  ]
}
```
```json
{
  "detail": "Premise limit exceeded: max 10 unvalidated contacts per premise"
}
```

### 7. Нефункциональные требования
* **Performance:** Валидация форматов — быстрая; проверка лимита — один запрос COUNT по помещению с индексом.

---

## CORE-03 — Конфликт владения (90 дней): архивация при валидации нового

### 1. Архитектурный анализ (CoT)
Реализуется в Logic Module при смене статуса контакта на «валидирован» (VAL-01): проверка даты валидации предыдущего «актуального» собственника по этому помещению. Если новый валидирован более чем через 90 дней после старого, старые данные переводятся в архив (отдельный статус или таблица). БД — PostgreSQL, триггер или сервисный слой.

### 2. Описание и цель
При валидации нового собственника по помещению: если прошло более 90 дней с момента валидации предыдущего, старые данные архивируются. Цель — актуализация данных и соответствие БТ [cite: 119, 120].

### 3. Функциональные требования
* **SR-CORE03-001:** При установке статуса «валидирован» для записи контакта по помещению система должна определить предыдущую запись по этому помещению со статусом «валидирован» и датой валидации.
* **SR-CORE03-002:** Если текущая дата минус дата валидации предыдущей записи &gt; 90 дней, система должна перевести предыдущую запись в статус «архив» (или аналог).
* **SR-CORE03-003:** Если разница ≤ 90 дней, система должна считать сособственниками (старая запись не архивируется; по БТ «в пределах 60 дней» — 🤔 допущение: для архивации порог 90 дней; для «сособственники» порог 60 дней — оставляем 90 для архивации).
* **SR-CORE03-004:** Операция архивации должна выполняться в той же транзакции, что и установка «валидирован» для новой записи.
* **SR-CORE03-005:** Система должна записывать в аудит-лог факт архивации (старая запись, причина: «новая валидация &gt;90 дней»).

### 4. Сценарий использования
**Триггер:** Администратор помечает контакт по помещению как «валидирован».
**Main Success Scenario:**
1. Админ выполняет валидацию (VAL-01).
2. Сервер проверяет 90-дневное правило, при необходимости архивирует старую запись, сохраняет новую валидацию.
**Alternative Flows:**
* **AF-1 (Нет предыдущей валидированной записи):** Только установка статуса новой записи.
* **AF-2 (Сбой БД):** Откат транзакции, возврат 503.

### 5. Модель данных
**Update:** Контакт — статус «архив» или отдельная таблица архивных записей. Поле даты валидации обязательно для расчёта 90 дней.

### 6. Интеграции и API
Внутренняя логика при PATCH /api/admin/contacts/{id}/status (VAL-01). Внешний API не меняется.

### 7. Нефункциональные требования
* **Consistency:** Транзакция; при ошибке архивации не применять валидацию.

---

## CORE-04 — Математика кворума: базовый расчёт долей (API)

### 1. Архитектурный анализ (CoT)
Реализуется в Logic Module: расчёт отношения площади помещений с голосами «ЗА» (всех, без требования валидации) к общей площади дома. Данные по площадям и голосам берутся из PostgreSQL. Результат отдаётся по API для отображения на фронте и в отчётах.

### 2. Описание и цель
Базовый прогноз кворума: доля площади с голосами «ЗА» от общей площади МКД. Цель — дать пользователю и админу показатель приближения к 2/3.

### 3. Функциональные требования
* **SR-CORE04-001:** Система должна вычислять общую площадь всех помещений МКД (по реестру помещений).
* **SR-CORE04-002:** Система должна вычислять сумму площадей помещений, по которым есть хотя бы одна запись с позицией «ЗА» (vote_for = true), без требования валидированности контакта.
* **SR-CORE04-003:** Система должна возвращать отношение (площадь «ЗА» / общая площадь) в виде числа (доля или процент) и при необходимости порог 2/3 (кворум достигнут да/нет).
* **SR-CORE04-004:** Система должна предоставлять API для получения этого расчёта по идентификатору дома/объекта.
* **SR-CORE04-005:** При отсутствии данных по помещению (нет реестра) система должна возвращать явное состояние «нет данных» или нули.

### 4. Сценарий использования
**Триггер:** Запрос страницы объекта или отчёта с прогнозом кворума.
**Main Success Scenario:**
1. Клиент запрашивает расчёт кворума по building_id.
2. Сервер считает площади и долю «ЗА», возвращает результат.
**Alternative Flows:**
* **AF-1 (Нет помещений):** Возврат 0/0 и флаг «нет данных».
* **AF-2 (Делитель ноль):** Избегать деления на ноль; возвращать 0 или «нет данных».

### 5. Модель данных
Используются: помещения (площадь), контакты (vote_for, связь с помещением). Общая площадь дома — сумма площадей помещений или отдельное поле в справочнике дома.

### 6. Интеграции и API
* **Method:** `GET /api/buildings/{building_id}/quorum`
* **Response:**
```json
{
  "total_area": 4500.5,
  "area_voted_for": 1200.0,
  "ratio": 0.266,
  "quorum_threshold": 0.667,
  "quorum_reached": false
}
```

### 7. Нефункциональные требования
* **Performance:** Расчёт должен выполняться за приемлемое время (индексы по помещению, голосам); при большом объёме — кеш на короткий TTL.

---

## FE-01 — SVG-навигация: 3-уровневый компонент (id=cadastral_number)

### 1. Архитектурный анализ (CoT)
Реализуется на Frontend (React): три уровня — мастер-план (подъезды/паркинг), схема разреза (этажи), план этажа (квартиры/мм/кладовки). Элементы SVG содержат id, соответствующий кадастровому номеру или идентификатору помещения в БД. Данные для подсветки состояний запрашиваются с API (CORE-04, контакты по помещениям).

### 2. Описание и цель
Интерактивная SVG-карта для выбора помещения: мастер-план → схема разреза → план этажа. Цель — наглядный выбор помещения с привязкой к кадастровым номерам.

### 3. Функциональные требования
* **SR-FE01-001:** Компонент должен отображать первый уровень (мастер-план): выбор подъезда или паркинга в виде векторной графики (SVG).
* **SR-FE01-002:** После выбора подъезда/зоны компонент должен отображать второй уровень (схема разреза): выбор этажа.
* **SR-FE01-003:** После выбора этажа компонент должен отображать третий уровень (план этажа): выбор помещения (квартира/машиноместо/кладовка) с id, соответствующим кадастровому номеру или premise_id в БД.
* **SR-FE01-004:** Каждый кликабельный объект SVG должен иметь атрибут id, соответствующий идентификатору помещения в системе (кадастровый номер или внутренний id).
* **SR-FE01-005:** При выборе помещения система должна устанавливать текущее выбранное помещение для последующей передачи в форму (FE-04).
* **SR-FE01-006:** Компонент должен поддерживать pinch-to-zoom и pan для паркинга и кладовок (по БТ).

### 4. Сценарий использования
**Триггер:** Пользователь открывает страницу с картой объекта.
**Main Success Scenario:**
1. Пользователь выбирает подъезд → этаж → помещение на плане.
2. Система фиксирует выбор и даёт возможность перейти к форме анкеты.
**Alternative Flows:**
* **AF-1 (Нет SVG для объекта):** Отображать сообщение «Карта недоступна» и ссылку на каскадные фильтры (FE-03).
* **AF-2 (Медленная загрузка):** Показывать скелетон/лоадер до появления SVG.

### 5. Модель данных
Не меняется; используются premise_id / cadastral_number из реестра. SVG-файлы или конфигурация слоёв должны содержать маппинг id элементов к этим идентификаторам.

### 6. Интеграции и API
Клиент запрашивает метаданные карты и состояния помещений (для легенды FE-02), например GET /api/buildings/{id}/premises/state или данные в составе ответа по объекту.

### 7. Нефункциональные требования
* **Accessibility:** Текстовая альтернатива — каскадные фильтры (FE-03). SVG с aria-label где применимо.
* **Performance:** Ленивая загрузка уровней; не грузить все этажи сразу при открытии мастер-плана.

---

## FE-02 — Легенда: динамическая окраска 5 состояний

### 1. Архитектурный анализ (CoT)
Реализуется на Frontend поверх SVG (FE-01): цвет контура/заливки в зависимости от состояния контактов по помещению. Состояния: нет данных (контур), есть невалидированные (светло-жёлтый), часть валидирована (светло-зелёный), все валидированы (ярко-зелёный), текущий выбор (ярко-синяя рамка). Данные состояний — с Backend (агрегат по помещениям).

### 2. Описание и цель
Визуальная легенда на карте: 5 состояний помещений для быстрой оценки охвата и валидации. Цель — соответствие БТ по легенде и UX.

### 3. Функциональные требования
* **SR-FE02-001:** Система должна отображать контур помещения без заливки (или нейтральный цвет), если по помещению нет контактов.
* **SR-FE02-002:** Система должна отображать помещение светло-жёлтым, если есть контакты и ни один не в статусе «валидирован».
* **SR-FE02-003:** Система должна отображать помещение светло-зелёным, если часть контактов валидирована, но не все.
* **SR-FE02-004:** Система должна отображать помещение ярко-зелёным, если все контакты по помещению валидированы.
* **SR-FE02-005:** Система должна выделять текущее выбранное пользователем помещение ярко-синей рамкой.
* **SR-FE02-006:** Клиент должен получать состояния помещений с сервера (API) и применять стили к элементам SVG по id помещения.

### 4. Сценарий использования
**Триггер:** Загрузка плана этажа или обновление данных после отправки анкеты/валидации.
**Main Success Scenario:**
1. Клиент запрашивает состояния по списку помещений текущего уровня.
2. Сервер возвращает для каждого premise_id состояние (none / unvalidated / partial / full).
3. Клиент применяет цвет по легенде и выделяет выбранное помещение синей рамкой.
**Alternative Flows:**
* **AF-1 (Нет данных о состоянии):** Считать состояние «нет данных» (контур).
* **AF-2 (Несколько контактов с разным статусом):** Правило «часть валидирована» — хотя бы один валидирован и хотя бы один не валидирован; «все валидированы» — все активные записи валидированы.

### 5. Модель данных
Не меняется; на сервере — агрегация по помещению: есть ли контакты, количество валидированных vs всего.

### 6. Интеграции и API
* **Method:** `GET /api/buildings/{building_id}/premises/state` или по этажу/подъезду.
* **Response:**
```json
{
  "premises": [
    { "premise_id": "uuid_or_cadastral", "state": "full" },
    { "premise_id": "...", "state": "partial" }
  ]
}
```
Состояния: `none` | `unvalidated` | `partial` | `full`. Текущий выбор — только на клиенте (синяя рамка).

### 7. Нефункциональные требования
* **Performance:** Один запрос на уровень/этаж; при большом числе помещений — батч или пагинация по видимой области.

---

## FE-05 — Cross-object: sessionStorage и Nudge-модал

### 1. Архитектурный анализ (CoT)
Реализуется на Frontend: использование sessionStorage для автозаполнения данных между переходами по помещениям; после сохранения первой анкеты — модальное окно (Nudge) с предложением выбрать другие помещения (машиноместо/кладовка). Архитектура не меняется; данные не уходят на сервер кроме явной отправки формы.

### 2. Описание и цель
Удержание пользователя и упрощение ввода: сохранение введённых данных в сессии и подсказка заполнить анкеты по другим помещениям того же собственника. Цель — повышение охвата и соответствие БТ [cite: 56, 57].

### 3. Функциональные требования
* **SR-FE05-001:** Система должна сохранять в sessionStorage данные, введённые в форму (кроме чувствительных по политике — 🤔 допущение: сохранять маску телефона/никнейм для подсказки, не полный телефон в открытом виде), для автозаполнения при переходе к другому помещению.
* **SR-FE05-002:** При открытии формы для нового помещения система должна подставлять из sessionStorage значения полей, ранее введённых в этой сессии (если применимо).
* **SR-FE05-003:** После успешной отправки первой анкеты система должна показывать модальное окно (Nudge) с текстом о возможности указать другие помещения (машиноместо, кладовка).
* **SR-FE05-004:** Nudge должен содержать кнопки/ссылки: перейти к выбору другого помещения или закрыть.
* **SR-FE05-005:** Закрытие Nudge не должно блокировать дальнейшую работу; пользователь может снова выбрать помещение через карту или фильтры.

### 4. Сценарий использования
**Триггер:** Пользователь отправил первую анкету или перешёл к форме другого помещения.
**Main Success Scenario:**
1. После отправки анкеты показывается Nudge; пользователь нажимает «Выбрать другое помещение» и выбирает машиноместо.
2. Форма открывается с подставленными из sessionStorage данными; пользователь при необходимости правит и отправляет.
**Alternative Flows:**
* **AF-1 (Пользователь закрыл Nudge):** Модал закрывается, карта/фильтры остаются доступны.
* **AF-2 (Очистка сессии):** При закрытии вкладки sessionStorage очищается; при новом визите автозаполнения нет.

### 5. Модель данных
Не меняется на сервере. На клиенте — ключи sessionStorage для полей формы (список ключей определить; не хранить полные ПДн в открытом виде).

### 6. Интеграции и API
Нет новых API; поведение только на клиенте.

### 7. Нефункциональные требования
* **Security:** Не сохранять в sessionStorage полные телефон/email в открытом виде; только подсказки для UX или хеш — 🤔 допущение: маска + последние цифры или «заполнено» флаг.

---

## BOT-01 — Алгоритмы поиска: Fuzzy Search (Левенштейн) и Regex кадастровых номеров

### 1. Архитектурный анализ (CoT)
Реализуется на Backend (отдельный сервис или модуль для бота): нормализация ввода, поиск по кадастровому номеру через regex, парсинг типа помещения и номера по словарю, fuzzy matching по Левенштейну. Используется Telegram-ботом для текстового поиска помещения («кв 45», «мместо 12»). Данные из той же PostgreSQL (реестр помещений).

### 2. Описание и цель
Распознавание намерения пользователя в текстовом вводе: извлечение кадастрового номера или «тип + номер» помещения с учётом опечаток. Цель — поиск помещения в боте по БТ [cite: 83, 84, 140–150, 180–181].

### 3. Функциональные требования
* **SR-BOT01-001:** Система должна нормализовать ввод: нижний регистр, удаление лишних пробелов, замена «ё» на «е».
* **SR-BOT01-002:** Система должна проверять ввод на соответствие шаблону кадастрового номера (regex) и при совпадении выполнять поиск помещения по кадастровому номеру в БД.
* **SR-BOT01-003:** Система должна парсить тип помещения (квартира, машиноместо, кладовка) и номер по словарю синонимов (кв, квартира, мм, мместо, кладовка и т.д.).
* **SR-BOT01-004:** Система должна применять нечёткое сравнение (расстояние Левенштейна или аналог) для сопоставления ввода со списком помещений при опечатках.
* **SR-BOT01-005:** Система должна возвращать лучшие совпадения (один или несколько) с идентификатором помещения (premise_id) для последующего использования в боте.
* **SR-BOT01-006:** При отсутствии совпадений система должна возвращать явный результат «не найдено».

### 4. Сценарий использования
**Триггер:** Пользователь бота вводит текст («кв 45», «мместо 5»).
**Main Success Scenario:**
1. Бот передаёт текст в сервис распознавания.
2. Сервис нормализует, проверяет КН, парсит тип/номер, при необходимости fuzzy match.
3. Возвращается список совпадений; бот предлагает выбрать помещение или переходит к анкете.
**Alternative Flows:**
* **AF-1 (Неоднозначный ввод):** Возврат нескольких вариантов; бот просит уточнить.
* **AF-2 (Некорректный ввод):** Возврат «не найдено»; бот предлагает ввести снова или перейти к каскадному выбору (если есть в боте).

### 5. Модель данных
Используется реестр помещений (CORE-01). Словарь синонимов для типов — конфиг или таблица.

### 6. Интеграции и API
* **Method:** `POST /api/bot/resolve-premise` (внутренний или с проверкой источника запросов от бота).
* **Request:**
```json
{
  "text": "кв 45",
  "building_id": "optional_for_scope"
}
```
* **Response:**
```json
{
  "matches": [
    { "premise_id": "uuid", "display": "Квартира 45", "confidence": 1.0 }
  ]
}
```

### 7. Нефункциональные требования
* **Performance:** Fuzzy search по подмножеству помещений (по дому); лимит на длину ввода и количество возвращаемых совпадений.

---

## BOT-02 — Бот-анкета: линейный диалог, Share Contact, согласия

### 1. Архитектурный анализ (CoT)
Реализуется в Telegram-боте (отдельный процесс или webhook): пошаговый сценарий (выбор помещения → собственник → контакт → позиция ЗА → формат → Электронный дом → согласия). Данные отправляются в тот же Backend (POST-контракт, совместимый с веб-формой или отдельный эндпоинт для бота). Share Contact — кнопка Telegram для передачи телефона.

### 2. Описание и цель
Линейный опрос в Telegram для сбора тех же данных, что и веб-форма: контакт, позиция, согласия. Цель — альтернативный канал по БТ [cite: 80, 81, 92, 93].

### 3. Функциональные требования
* **SR-BOT02-001:** Бот должен вести пользователя по шагам: выбор помещения (через BOT-01 или список) → чекбокс «Я собственник» → ввод/передача контакта → позиция ЗА (Да/Нет) → формат (Бумага/Электронно) → «Зарегистрирован в Электронном Доме» → подтверждение согласий на ОПД.
* **SR-BOT02-002:** Бот должен поддерживать кнопку Share Contact (Telegram) для передачи номера телефона.
* **SR-BOT02-003:** Бот должен запрашивать явное согласие на обработку ПДн (текст + подтверждение) перед сохранением.
* **SR-BOT02-004:** После сбора всех данных бот должен отправлять их на Backend (API) в формате, позволяющем создать запись контакта (аналог POST /api/submit с меткой источника «telegram»).
* **SR-BOT02-005:** При успешной отправке бот должен подтверждать пользователю сохранение; при ошибке — сообщать о необходимости повторить или связаться с инициативной группой.
* **SR-BOT02-006:** Капча для бота не требуется (🤔 допущение: идентификация по Telegram id считается достаточной для бота; rate limit по telegram_id применить).

### 4. Сценарий использования
**Триггер:** Пользователь запускает бота и начинает диалог.
**Main Success Scenario:**
1. Пользователь выбирает помещение, вводит/передаёт контакт, отвечает на вопросы, подтверждает согласия.
2. Бот отправляет данные на сервер; пользователь получает подтверждение.
**Alternative Flows:**
* **AF-1 (Отмена на шаге):** Пользователь может отменить; бот сбрасывает состояние сессии для этого чата.
* **AF-2 (Ошибка API/лимит):** Бот сообщает об ошибке и предлагает повторить позже.

### 5. Модель данных
Та же модель контакта (FE-04); поле source = "telegram", telegram_id отправителя для связи.

### 6. Интеграции и API
* **Method:** `POST /api/submit` с телом, расширенным полем `source: "telegram"` и при необходимости `telegram_user_id`. Либо отдельный `POST /api/bot/submit` с тем же контрактом данных.
* **Request:** Аналогично FE-04; captcha_token может быть пустым или не передаваться при source=telegram (проверка на Backend).

### 7. Нефункциональные требования
* **Security:** Взаимодействие бота с Backend по защищённому каналу; проверка подписи Telegram Webhook при использовании webhook.
* **Compliance:** Согласия хранить в той же структуре, что и для веб (версия политики, флаг согласия).

---

## BOT-03 — Личный кабинет в боте: редактирование и «Право на забвение»

### 1. Архитектурный анализ (CoT)
Реализуется в том же Telegram-боте: меню «Мои данные» / «Редактировать» и «Удалить мои данные». Редактирование — обновление своей записи через API (идентификация по telegram_id). Удаление — запрос на удаление ПДн (право на забвение по БТ); Backend помечает запись к удалению или удаляет и фиксирует в аудите.

### 2. Описание и цель
Возможность пользователя в боте редактировать ранее поданные данные и отозвать согласие (удаление данных). Цель — соответствие БТ [cite: 92, 93] и 152-ФЗ.

### 3. Функциональные требования
* **SR-BOT03-001:** Бот должен предоставлять пункт меню (или команду) «Редактировать мои данные».
* **SR-BOT03-002:** При выборе редактирования бот должен запрашивать выбор помещения (из списка помещений, по которым пользователь ранее оставил контакт по этому telegram_id) или ввод помещения.
* **SR-BOT03-003:** Система должна предоставлять API для обновления записи контакта при условии, что запрос авторизован от того же telegram_id, который указан в записи (или привязан к ней).
* **SR-BOT03-004:** Бот должен предоставлять пункт меню (или команду) «Удалить мои данные» / «Право на забвение».
* **SR-BOT03-005:** При запросе удаления система должна помечать запись как удалённую (soft delete) или удалять ПДн, оставляя метаданные для аудита (кто, когда удалил).
* **SR-BOT03-006:** Система должна подтверждать пользователю в боте выполнение удаления/обновления.

### 4. Сценарий использования
**Триггер:** Пользователь открывает меню бота и выбирает «Редактировать» или «Удалить данные».
**Main Success Scenario:**
1. Пользователь выбирает «Редактировать», выбирает помещение, вводит новые данные; бот отправляет PATCH на Backend.
2. Backend проверяет принадлежность записи пользователю (telegram_id), обновляет запись, возвращает успех.
**Alternative Flows:**
* **AF-1 (Запись не найдена / не принадлежит пользователю):** Backend возвращает 404/403; бот сообщает «Данные не найдены».
* **AF-2 (Удаление):** Пользователь подтверждает удаление; Backend выполняет soft delete или обнуление ПДн; бот подтверждает.

### 5. Модель данных
**Update:** Контакт — флаг deleted или дата удаления; при soft delete контакты не отображаются в отчётах и на карте. Аудит: запись о действии «удаление по запросу субъекта».

### 6. Интеграции и API
* **Method:** `PATCH /api/bot/me/contacts/{contact_id}` — тело с обновляемыми полями; авторизация по Telegram (токен бота или telegram_id в заголовке после проверки).
* **Method:** `DELETE /api/bot/me/contacts/{contact_id}` или `POST /api/bot/me/forget` с указанием contact_id/помещений.
* **Response:** 200 OK с сообщением об успехе.

### 7. Нефункциональные требования
* **Security:** Строгая привязка операций к telegram_id; нельзя редактировать/удалять чужие записи.
* **Audit:** Все запросы на удаление и редактирование логировать (BE-03).

---

## BOT-04 — Синхронизация: Webhook/Polling для обновления в БД и на SVG

### 1. Архитектурный анализ (CoT)
Данные, внесённые через бота, сохраняются в общую БД (BOT-02); веб-клиент получает обновления либо по повторному запросу состояний (FE-02), либо через периодический опрос/SSE. В архитектуре не заложен отдельный real-time канал (WebSocket); «мгновенно» по БТ интерпретируется как быстрая доступность при следующем запросе или короткий интервал опроса.

### 2. Описание и цель
Обеспечение отображения на SVG-карте и в отчётах данных, внесённых через Telegram-бота, без ручного обновления страницы. Цель — синхронизация каналов по БТ [cite: синхронизация].

### 3. Функциональные требования
* **SR-BOT04-001:** Данные, созданные/обновлённые через бота (BOT-02, BOT-03), должны сохраняться в той же БД, что и веб-анкеты.
* **SR-BOT04-002:** Веб-клиент при запросе состояний помещений (для легенды FE-02) или расчёта кворума (CORE-04) должен получать актуальные данные, включая записи из бота.
* **SR-BOT04-003:** Система должна обеспечивать обновление отображения на карте в течение разумного времени (🤔 допущение: при следующем запросе клиента к API состояний — без отдельного WebSocket; при необходимости — опрос не реже чем раз в N секунд при открытой карте).
* **SR-BOT04-004:** Если реализован механизм опроса (polling), клиент должен запрашивать обновление состояний помещений с интервалом, заданным конфигурацией (например, 30–60 сек), при активном просмотре карты.

### 4. Сценарий использования
**Триггер:** Пользователь внёс данные через бота; другой пользователь или админ смотрит карту в браузере.
**Main Success Scenario:**
1. Данные из бота сохранены в БД.
2. При следующей загрузке страницы или следующем запросе состояний веб-клиент получает обновлённые цвета на карте и актуальный кворум.
**Alternative Flows:**
* **AF-1 (Задержка):** Без real-time — задержка до следующего запроса клиента; при опросе — до одного интервала опроса.
* **AF-2 (Недоступность БД):** По OPS-02 — graceful degradation; карта может показывать кешированные данные или заглушку.

### 5. Модель данных
Не меняется; единая БД для веб и бота.

### 6. Интеграции и API
Используются существующие API: GET состояний помещений (FE-02), GET кворума (CORE-04). Дополнительно: опциональный эндпоинт «последнее обновление» для уменьшения трафика при polling (If-Modified-Since или timestamp).

### 7. Нефункциональные требования
* **Performance:** Опрос не создавать избыточную нагрузку (интервал и кеш на Backend).
* 🤔 **Допущение:** Полноценный WebSocket в базовой архитектуре не предусмотрен; при требовании «мгновенно» рассмотреть SSE или WebSocket в следующей итерации.

---

## ADM-02 — Интерфейс модерации: таблица, фильтры IP/TimeRange, пакетный перевод в «Неактуальные»

### 1. Архитектурный анализ (CoT)
Реализуется на Frontend (React) + Backend: страница для администратора с таблицей записей контактов, фильтрами по IP и диапазону дат, действием «перевести выбранные в Неактуальные». Backend предоставляет GET с фильтрами и PATCH для пакетного обновления статуса. Аудит (BE-03) фиксирует массовые действия.

### 2. Описание и цель
Интерфейс для пакетной модерации записей: просмотр с фильтрами и массовый перевод в «Неактуальные» для защиты от спама. Цель — БТ [cite: 72, 74].

### 3. Функциональные требования
* **SR-ADM02-001:** Система должна предоставлять API для получения списка записей контактов с фильтрами: IP-адрес (или маска), диапазон дат (TimeRange).
* **SR-ADM02-002:** Система должна возвращать в списке только метаданные и обезличенные идентификаторы (не расшифрованные контакты в списке по умолчанию — или по политике доступа с аудитом чтения).
* **SR-ADM02-003:** Система должна предоставлять API для пакетного перевода выбранных записей в статус «Неактуальные» (массив contact_id).
* **SR-ADM02-004:** Пакетное обновление должно выполняться в транзакции и записываться в аудит-лог (одна запись на действие «пакетная модерация» с перечнем id).
* **SR-ADM02-005:** Клиент должен отображать таблицу с выбранными фильтрами, чекбоксы выбора записей и кнопку «Перевести в Неактуальные».

### 4. Сценарий использования
**Триггер:** Администратор открывает раздел модерации.
**Main Success Scenario:**
1. Администратор задаёт фильтры (IP, даты), загружает список, выбирает записи и нажимает «В Неактуальные».
2. Система обновляет статусы и возвращает успех; таблица обновляется.
**Alternative Flows:**
* **AF-1 (Нет выбранных записей):** Кнопка неактивна или возвращается ошибка «Выберите хотя бы одну запись».
* **AF-2 (Таймаут при большом выборе):** Ограничить размер пакета (например, до 500 id); при превышении — разбить на батчи или предупреждение.

### 5. Модель данных
Используется модель контакта (статус). Аудит — запись о пакетном действии.

### 6. Интеграции и API
* **Method:** `GET /api/admin/contacts?ip=&from=&to=&page=&size=`
* **Response:** Список с contact_id, premise_id, ip, created_at, status (без расшифрованных ПДн в списке или с ограничением прав).
* **Method:** `POST /api/admin/contacts/bulk-status`
* **Request:**
```json
{
  "contact_ids": ["uuid1", "uuid2"],
  "status": "inactive"
}
```

### 7. Нефункциональные требования
* **Security:** Только роль administrator/super_administrator; аудит всех пакетных действий.
* **Performance:** Пагинация и лимит размера ответа; индексы по ip, created_at.

---

## ADM-03 — Прямой ввод: форма добавления контактов админом (авто-валидация)

### 1. Архитектурный анализ (CoT)
Реализуется в админ-интерфейсе (React) и Logic Module: форма ввода контакта по выбранному помещению с автоматической установкой статуса «валидирован» (т.к. ввод от доверенного оператора). Используются те же валидации форматов (CORE-02), шифрование (BE-02), лимиты на помещение при необходимости не применяются или ослаблены для админа.

### 2. Описание и цель
Возможность администратора вручную добавить контакт по помещению с немедленной валидацией. Цель — ввод данных из бумажных анкет или по телефону.

### 3. Функциональные требования
* **SR-ADM03-001:** Система должна предоставлять форму (UI) и API для создания записи контакта от имени администратора с привязкой к помещению.
* **SR-ADM03-002:** При создании контакта через админ-форму система должна автоматически устанавливать статус «валидирован».
* **SR-ADM03-003:** Система должна применять валидацию форматов полей (телефон, email, telegram) по правилам CORE-02.
* **SR-ADM03-004:** Система должна сохранять контакты в зашифрованном виде (BE-02) и записывать в аудит факт создания и идентификатор админа.
* **SR-ADM03-005:** Система не должна требовать капчу для запроса от администратора; проверка JWT достаточна.

### 4. Сценарий использования
**Триггер:** Администратор выбирает «Добавить контакт» и выбирает помещение.
**Main Success Scenario:**
1. Администратор заполняет поля контакта и позиции по ОСС, отправляет форму.
2. Сервер проверяет права и форматы, шифрует, сохраняет со статусом «валидирован», возвращает успех.
**Alternative Flows:**
* **AF-1 (Ошибка валидации):** Возврат 400 с указанием поля.
* **AF-2 (Помещение не найдено):** Возврат 404.

### 5. Модель данных
Та же сущность контакта; источник/флаг admin_created для аудита.

### 6. Интеграции и API
* **Method:** `POST /api/admin/contacts`
* **Request:**
```json
{
  "premise_id": "uuid",
  "phone": "+7...",
  "email": null,
  "telegram_id": null,
  "vote_for": true,
  "vote_format": "paper",
  "registered_ed": false
}
```
* **Response:** 201 Created с contact_id и статусом validated.

### 7. Нефункциональные требования
* **Security:** Только администратор; аудит создания с user_id.
* **Compliance:** Согласие на ОПД при ручном вводе — 🤔 допущение: фиксировать в записи «согласие получено при личном контакте» или требовать чекбокс в форме.

---

## ADM-04 — Super-admin UI: добавление/удаление администраторов

### 1. Архитектурный анализ (CoT)
Реализуется в Auth Module и админ-интерфейсе: только суперадмин может добавлять/удалять записи в белом списке (БД или конфиг). UI — форма/таблица и вызов API; Backend проверяет роль super_administrator.

### 2. Описание и цель
Управление составом администраторов: добавление и удаление учётных записей из белого списка Telegram. Цель — БТ раздел 2.3.

### 3. Функциональные требования
* **SR-ADM04-001:** Система должна предоставлять API для добавления записи в белый список админов (telegram_id, роль administrator) только при вызове от пользователя с ролью super_administrator.
* **SR-ADM04-002:** Система должна предоставлять API для удаления записи из белого списка (по telegram_id) только от суперадмина.
* **SR-ADM04-003:** Суперадмин не может удалить сам себя через UI (или допускается с предупреждением — 🤔 допущение: разрешить удаление с потерей доступа до следующего добавления другим суперадмином).
* **SR-ADM04-004:** Клиент должен отображать список текущих администраторов и кнопки «Добавить» / «Удалить» (доступно только суперадмину).

### 4. Сценарий использования
**Триггер:** Суперадмин открывает раздел управления админами.
**Main Success Scenario:**
1. Суперадмин вводит Telegram username/id и добавляет администратора; система сохраняет в белый список.
2. При следующем входе новый админ получает JWT с ролью administrator.
**Alternative Flows:**
* **AF-1 (Попытка не суперадмина):** API возвращает 403.
* **AF-2 (Удаление последнего суперадмина):** Запретить или предупреждение с подтверждением.

### 5. Модель данных
**Update:** Белый список: telegram_id, role (administrator | super_administrator), created_at. Минимум один super_administrator должен существовать.

### 6. Интеграции и API
* **Method:** `GET /api/superadmin/admins` — список (без выдачи чувствительных данных).
* **Method:** `POST /api/superadmin/admins` — тело: { "telegram_id": "...", "role": "administrator" }.
* **Method:** `DELETE /api/superadmin/admins/{telegram_id}`.

### 7. Нефункциональные требования
* **Security:** Строгая проверка роли; все действия логировать (ADM-05).

---

## ADM-05 — Логирование действий суперадмина

### 1. Архитектурный анализ (CoT)
Расширение аудит-лога (BE-03): все действия суперадмина (добавление/удаление админов, возможно другие) записываются в тот же механизм аудита с пометкой роли и сущности.

### 2. Описание и цель
Аудит действий суперадмина для подотчётности и расследования инцидентов.

### 3. Функциональные требования
* **SR-ADM05-001:** Система должна записывать в аудит-лог каждое добавление записи в белый список админов (кто добавил, кого, когда).
* **SR-ADM05-002:** Система должна записывать в аудит-лог каждое удаление записи из белого списка (кто удалил, кого, когда).
* **SR-ADM05-003:** Запись лога должна содержать: идентификатор суперадмина (telegram_id или user_id), действие, целевой telegram_id, IP, метку времени.
* **SR-ADM05-004:** Просмотр логов суперадминских действий должен быть доступен только суперадмину (или отдельный экспорт для комплаенса).

### 4. Сценарий использования
**Триггер:** Суперадмин выполняет добавление или удаление администратора (ADM-04).
**Main Success Scenario:**
1. Суперадмин вызывает POST/DELETE админов.
2. После успешного выполнения система записывает событие в audit_log.
**Alternative Flows:**
* **AF-1 (Сбой записи лога):** По политике BE-03 — не блокировать основную операцию; fallback в файл при недоступности БД логов.

### 5. Модель данных
Используется таблица audit_log; entity_type = "admin_whitelist", operation = ADD/REMOVE.

### 6. Интеграции и API
Внутренняя реализация; API просмотра логов — фильтр по entity_type и role.

### 7. Нефункциональные требования
* **Retention:** Политика хранения логов суперадмина не короче общего аудита (документировать).

---

## OPS-01 — Backups: ежедневные дампы на S3 (глубина 14 дней)

### 1. Архитектурный анализ (CoT)
Реализуется вне основного приложения: скрипт/задача (cron или scheduler в контейнере), выполняющая pg_dump (или аналог) и загрузку в S3-совместимое хранилище. В архитектуре S3 не указан — 🤔 допущение: использование S3-совместимого хранилища в РФ (например, VK Cloud, Selectel) не противоречит «не выдумывать новые компоненты», т.к. это операционное хранилище, не логика приложения.

### 2. Описание и цель
Ежедневное резервное копирование БД на внешнее хранилище с ротацией 14 дней. Цель — БТ [cite: 131, 133] и RTO.

### 3. Функциональные требования
* **SR-OPS01-001:** Система (скрипт/задача) должна выполнять создание дампа БД (полный или по выбранной схеме) не реже одного раза в сутки.
* **SR-OPS01-002:** Дамп должен загружаться в хранилище, доступное по протоколу S3 (или S3-совместимому API).
* **SR-OPS01-003:** В хранилище должна сохраняться глубина не менее 14 копий (14 дней при ежедневном запуске) с удалением более старых.
* **SR-OPS01-004:** Доступ к хранилищу должен осуществляться по учётным данным (ключи/секреты), хранящимся вне репозитория.
* **SR-OPS01-005:** При сбое создания дампа или загрузки система должна логировать ошибку и при необходимости уведомлять ответственных (см. OPS-03).

### 4. Сценарий использования
**Триггер:** По расписанию (ежедневно).
**Main Success Scenario:**
1. Задача запускается, создаёт дамп PostgreSQL, загружает в S3, удаляет копии старше 14 дней.
2. Лог фиксирует успех.
**Alternative Flows:**
* **AF-1 (Недоступность БД):** Дамп не создаётся; логировать; не удалять последнюю успешную копию.
* **AF-2 (Недоступность S3):** Логировать; повторить при следующем запуске или по политике retry.

### 5. Модель данных
Не применимо.

### 6. Интеграции и API
Внешняя интеграция: S3 API (PutObject, ListObjects, DeleteObject). Локально: вызов pg_dump.

### 7. Нефункциональные требования
* **Security:** Шифрование дампа при хранении (S3 server-side или клиентское) и ограничение доступа по IAM/политикам.
* **Compliance:** Хранилище в РФ при требовании локализации данных.

---

## OPS-02 — Отказоустойчивость: страницы-заглушки (Graceful Degradation)

### 1. Архитектурный анализ (CoT)
Реализуется на Frontend и при необходимости на Backend: при недоступности БД или критичного сервиса возвращать страницу-заглушку или ответ с понятным сообщением вместо сырой ошибки. Цель — БТ [cite: 134, 135] и приемлемый UX при сбоях.

### 2. Описание и цель
Обеспечение предсказуемого поведения при сбоях: отображение заглушки «Сервис временно недоступен» вместо падения приложения или стека ошибок.

### 3. Функциональные требования
* **SR-OPS02-001:** При недоступности БД backend должен возвращать HTTP 503 (или аналог) с телом, не содержащим внутренних деталей (стек, пути).
* **SR-OPS02-002:** Frontend при получении 503 (или при таймауте критичных запросов) должен отображать страницу-заглушку с сообщением о временной недоступности и рекомендацией повторить позже.
* **SR-OPS02-003:** Заглушка не должна требовать доступа к API для отображения (статическая или встроенная в бандл).
* **SR-OPS02-004:** При восстановлении сервиса пользователь при обновлении страницы должен снова получать рабочее приложение.

### 4. Сценарий использования
**Триггер:** БД недоступна или таймаут при обращении к backend.
**Main Success Scenario:**
1. Пользователь открывает приложение; backend возвращает 503.
2. Frontend показывает заглушку «Сервис временно недоступен».
**Alternative Flows:**
* **AF-1 (Частичная недоступность):** Отдельные эндпоинты могут возвращать 503; главная страница и статика остаются отдаваться (Nginx).
* **AF-2 (Восстановление):** После восстановления БД ответы 200; заглушка больше не показывается.

### 5. Модель данных
Не применимо.

### 6. Интеграции и API
Ответ backend при недоступности БД:
```json
{
  "detail": "Service temporarily unavailable"
}
```
Status: 503.

### 7. Нефункциональные требования
* **Security:** В ответах не раскрывать версии ПО и внутренние пути.
* **UX:** Текст заглушки на русском, нейтральный тон.

---

## CORE-06 — Математика кворума: сложный расчёт долей (API)

### 1. Архитектурный анализ (CoT)
Расширение Logic Module (CORE-04): расчёт доли площади, где *все* валидированные собственники по помещению проголосовали «ЗА», к общей площади дома. Учитываются только помещения с полной валидацией и единогласно «ЗА». Данные из той же БД.

### 2. Описание и цель
Сложный вариант прогноза кворума: в знаменатель идут только площади помещений, по которым все валидированные контакты — «ЗА». Цель — консервативная оценка по БТ.

### 3. Функциональные требования
* **SR-CORE06-001:** Система должна определять помещения, по которым все активные (не удалённые, не неактуальные) валидированные контакты имеют vote_for = true.
* **SR-CORE06-002:** Система должна суммировать площади только таких помещений.
* **SR-CORE06-003:** Система должна вычислять отношение этой суммы к общей площади МКД и возвращать долю и флаг достижения порога 2/3.
* **SR-CORE06-004:** Система должна предоставлять API для получения этого расчёта по идентификатору дома (отдельный эндпоинт или параметр mode=strict в CORE-04).
* **SR-CORE06-005:** Помещения без валидированных контактов или с хотя бы одним «против» не входят в сумму для сложного расчёта.

### 4. Сценарий использования
**Триггер:** Запрос отчёта или страницы аналитики с «строгим» расчётом кворума.
**Main Success Scenario:**
1. Клиент запрашивает сложный расчёт по building_id.
2. Сервер считает площадь «все валидированы и ЗА», возвращает ratio и quorum_reached.
**Alternative Flows:**
* **AF-1 (Нет помещений с полным согласием):** Возврат ratio = 0, quorum_reached = false.
* **AF-2 (Делитель ноль):** Аналогично CORE-04 — избегать деления на ноль.

### 5. Модель данных
Используются помещения, контакты (status, vote_for). Логика: по каждому помещению проверить «все валидированные = ЗА».

### 6. Интеграции и API
* **Method:** `GET /api/buildings/{building_id}/quorum?mode=strict`
* **Response:** Тот же формат, что в CORE-04; значения считаются по правилам «только единогласно валидированные ЗА».

### 7. Нефункциональные требования
* **Performance:** Возможна более тяжёлая агрегация; индексы и при необходимости кеш с коротким TTL.

---

## OPS-03 — Мониторинг: Uptime-check бот для уведомления админов

### 1. Архитектурный анализ (CoT)
Внешний мониторинг: периодический запрос к health-эндпоинту приложения (или главной страницы); при недоступности — отправка уведомления администраторам (Telegram-бот или email). В базовой архитектуре отдельный сервис не описан — допущение: скрипт или отдельный лёгкий сервис (cron + curl + Telegram Bot API) не противоречит ограничению «не выдумывать новые БД/очереди».

### 2. Описание и цель
Своевременное оповещение ответственных о недоступности приложения. Цель — сокращение времени обнаружения сбоев.

### 3. Функциональные требования
* **SR-OPS03-001:** Система (скрипт или сервис) должна периодически выполнять проверку доступности приложения (HTTP GET к health-эндпоинту или главной странице).
* **SR-OPS03-002:** При получении не-2xx ответа или таймауте система должна инициировать уведомление ответственным (например, в Telegram-чат или группу админов).
* **SR-OPS03-003:** Backend должен предоставлять эндпоинт для проверки живости (например, GET /health возвращающий 200 при доступности БД).
* **SR-OPS03-004:** Содержимое уведомления не должно содержать чувствительных данных; достаточно «Сервис Кворум-МКД недоступен, время X».

### 4. Сценарий использования
**Триггер:** По расписанию (каждые 5–15 мин).
**Main Success Scenario:**
1. Проверка запрашивает /health; получает 200.
2. Никаких действий.
**Alternative Flows:**
* **AF-1 (Недоступность):** Получен 5xx или таймаут; отправлено уведомление в Telegram.
* **AF-2 (Ложное срабатывание):** Рекомендуется повторная проверка перед уведомлением (два подряд неуспеха) — 🤔 допущение.

### 5. Модель данных
Не применимо.

### 6. Интеграции и API
* **Method:** `GET /health` — ответ 200 OK, тело опционально (например, {"status": "ok", "db": "connected"}).
* Внешняя интеграция: Telegram Bot API для отправки сообщения в чат/группу.

### 7. Нефункциональные требования
* **Security:** Токен бота и ID чата хранить в конфигурации/секретах.
* **Availability:** Мониторинг не должен создавать существенную нагрузку (один запрос за интервал).

---
